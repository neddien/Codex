#include <engine/system/utils.h>

// NOTE: POSIX API doesn't mean compatibility between BSD, SysV, Linux, OSX.
// This directory exists only for compatible implementations between Linux <-> OSX.
// Codex doesn't support Unix and it's variants (FreeBSD, OpenBSD, Solaris etc).

namespace codex::sys {
} // namespace codex::sys
